name: Java CI with Maven
 
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    uses: compassion-technology/maven-parent/.github/workflows/maven.yml@main
    secrets:
      token: ${{ secrets.GH_PACKAGES_NPM_TOKEN }}